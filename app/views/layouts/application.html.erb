<!DOCTYPE html>
<html>
  <head>
    <!-- Title of the Page: If content_for(:title) is set, it will be used; otherwise, the default "Eventify2107" will be used -->
    <title><%= content_for(:title) || "Eventify2107" %></title>

    <!-- Meta Tags for Mobile Responsiveness and Apple Web App -->
    <meta name="viewport" content="width=device-width,initial-scale=1"> <!-- Ensures proper scaling on mobile devices -->
    <meta name="apple-mobile-web-app-capable" content="yes"> <!-- Allows the app to be used as a web app on iOS devices -->

    <!-- CSRF Token for security, generated by Rails -->
    <%= csrf_meta_tags %>

    <!-- Content Security Policy meta tag to define security policies for resources loaded -->
    <%= csp_meta_tag %>

    <!-- Yield allows for dynamic head content from individual views -->
    <%= yield :head %>

    <!-- Manifest and Icons for Progressive Web App (PWA) support -->
    <link rel="manifest" href="/manifest.json"> <!-- Web App Manifest -->
    <link rel="icon" href="/icon.png" type="image/png"> <!-- Favicon for the web app -->
    <link rel="icon" href="/icon.svg" type="image/svg+xml"> <!-- SVG Favicon for the web app -->
    <link rel="apple-touch-icon" href="/icon.png"> <!-- Apple touch icon for mobile devices -->

    <!-- Application Stylesheet Link -->
    <%= stylesheet_link_tag "application", "data-turbo-track": "reload" %> <!-- Main stylesheet linked with Turbo support -->

    <!-- JavaScript Importmap for Turbo support and other JavaScript files -->
    <%= javascript_importmap_tags %>

    <!-- Additional JavaScript files if any -->
    <%= javascript_include_tag "application", "data-turbo-track": "reload" %> <!-- Include the main JavaScript file -->
  </head>

  <body>
    <!-- Yielding to the body content of the individual views -->
    <%= yield %> <!-- This will render the content of individual pages -->
  </body>
</html>
